import styled from 'styled-components';

export default {
  Images: styled.div`
    width: 100%;
    display: grid;
    grid-gap: 5px;
    grid-template-columns: repeat(auto-fill, 300px);
    margin-bottom: 10px;
  `,
};
